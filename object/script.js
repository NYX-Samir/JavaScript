//object = an object is a collection of key-value pairs where keys are strings, and values can be any data type, including other objects or functions. Objects are used to store and manage data.

const person1 = {
    name:"Syntax Error",
    age:23,
    sex:"Male"
} 

console.log(person1.name);
console.log(person1.age);
console.log(person1.sex);